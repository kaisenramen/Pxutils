local commands = {}

commands.version = "1.0"

commands.colors = require("commands.colors")
commands.reduce = require("commands.reduce")
commands.size = require("commands.size")
commands.downscalenn = require("commands.downscalenn")
commands.downscaleni = require("commands.downscaleni")

return commands